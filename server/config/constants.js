// ==============================
// WHITE STARS
// Константы проекта
// ==============================

const PORT = process.env.PORT || 3000;

const ADMIN_KEY =
    process.env.ADMIN_KEY ||
    "white-stars-super-admin";

const APP_NAME = "WHITE STARS";

const VERSION = "5.0.0";

module.exports = {
    PORT,
    ADMIN_KEY,
    APP_NAME,
    VERSION
};